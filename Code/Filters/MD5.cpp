#include "MD5.h"

bool MD5::Encode(std::string &in, std::string &out) {
    //int64_t length = in.length();
    //Pad to sizeof(in)%512 = 448 bytes by adding '1' bit, and the '0's
    //Append length to input
    return true;
}

bool MD5::Decode(std::string &in, std::string &out) {
    return true;
}
